name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [windows-latest, macos-latest, ubuntu-22.04]
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libdbus-1-dev
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - run: npm ci
      # TEMP DEBUG: diagnose p12 import failure (remove after signing works)
      - name: Debug certificate import
        if: runner.os == 'macOS'
        env:
          APPLE_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
        run: |
          echo "secret length: $(printf '%s' "$APPLE_CERTIFICATE" | wc -c)"
          echo "without whitespace: $(printf '%s' "$APPLE_CERTIFICATE" | tr -d '[:space:]' | wc -c)"
          printf '%s' "$APPLE_CERTIFICATE" | base64 --decode > /tmp/cert.p12 || echo "DECODE FAILED"
          ls -l /tmp/cert.p12
          shasum -a 256 /tmp/cert.p12
          openssl version
          openssl pkcs12 -info -in /tmp/cert.p12 -noout -passin "pass:$APPLE_CERTIFICATE_PASSWORD" && echo "P12 PASSWORD OK" || echo "P12 PASSWORD REJECTED"
      # END TEMP DEBUG
      # Pull this version's section out of CHANGELOG.md for the release body.
      - name: Extract release notes from CHANGELOG
        id: notes
        shell: bash
        run: |
          version="${GITHUB_REF_NAME#v}"
          awk -v ver="$version" '
            $0 ~ "^## \\[" ver "\\]" { found = 1; next }
            found && /^## \[/ { exit }
            found && !/^\[/ { print }
          ' CHANGELOG.md > notes.md
          if [ ! -s notes.md ]; then
            echo "::warning::no CHANGELOG section found for $version"
            echo "See CHANGELOG.md for details." > notes.md
          fi
          {
            echo "body<<__NOTES_EOF__"
            cat notes.md
            echo "__NOTES_EOF__"
          } >> "$GITHUB_OUTPUT"
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # macOS signing + notarization. tauri-action's contract uses APPLE_*
          # names; the repo stores the certificate under MACOS_* names.
          APPLE_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Verkkokyylä v__VERSION__"
          releaseBody: ${{ steps.notes.outputs.body }}
          releaseDraft: false
          prerelease: false
      # Tauri has no "portable exe" bundle target; the raw binary is
      # self-contained apart from the WebView2 runtime (preinstalled on
      # up-to-date Windows 10/11). Zip it and attach it to the release.
      - name: Package standalone Windows exe
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".TrimStart("v")
          Compress-Archive -Path "src-tauri/target/release/verkkokyyla.exe" -DestinationPath "verkkokyyla_${version}_x64-standalone.zip"
      - name: Attach standalone exe to the release
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $version = "${{ github.ref_name }}".TrimStart("v")
          gh release upload "${{ github.ref_name }}" "verkkokyyla_${version}_x64-standalone.zip" --clobber
